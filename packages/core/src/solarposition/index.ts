/**
 * Solar position.
 *
 * Spec: NOAA SPA (Reda & Andreas, 2004) — zenith / azimuth / elevation.
 * Validation: pin reference outputs as fixtures and assert against them.
 *
 * TODO: implement SPA. Point-in-time first (scalar in/out); time-series batches
 * layer on top. Heavy numeric loops may move to an opt-in WASM accelerator post-1.0.
 */

export {};
