/**
 * Cell temperature.
 *
 * Spec: SAPM (Sandia Array Performance Model) temperature model, PVsyst model.
 * Input: POA irradiance, ambient temperature, wind speed → cell temperature.
 * Validation: pin reference outputs as fixtures and assert against them.
 *
 * TODO: SAPM first (curate the parameter table), then PVsyst.
 */

export {};
